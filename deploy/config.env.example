# ============================================
# Treasury Hugo Deployment Configuration
# ============================================
# Copy this file to config.env and fill in values
# cp config.env.example config.env
# ============================================

# ----- AWS S3 Configuration -----
AWS_REGION=us-east-1
S3_BUCKET_STAGING=treasury-hugo-staging
S3_BUCKET_PROD=treasury-hugo-prod

# Optional: CloudFront distribution ID (for cache invalidation)
CLOUDFRONT_DISTRIBUTION_ID=

# ----- Akamai Configuration -----
# Network: "staging" or "production"
AKAMAI_NETWORK=production

# Hostname to purge
AKAMAI_HOSTNAME=home.treasury.gov

# Akamai EdgeGrid credentials (or configure via ~/.edgerc)
# AKAMAI_CLIENT_TOKEN=
# AKAMAI_CLIENT_SECRET=
# AKAMAI_ACCESS_TOKEN=
# AKAMAI_HOST=
